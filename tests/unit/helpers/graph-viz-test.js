import {
  graphViz
} from 'peertopeer/helpers/graph-viz';

module('GraphVizHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = graphViz(42);
  ok(result);
});
